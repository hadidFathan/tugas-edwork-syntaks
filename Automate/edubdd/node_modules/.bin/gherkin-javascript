#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cucumber/node_modules/gherkin/bin/gherkin" "$@"
  ret=$?
else 
  node  "$basedir/../cucumber/node_modules/gherkin/bin/gherkin" "$@"
  ret=$?
fi
exit $ret
